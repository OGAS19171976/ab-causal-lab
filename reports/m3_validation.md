# M3 验证报告

```text
==========================================================================
ab-causal-lab · M3 验证报告
DiD（含交错处置）· 合成控制 · 平行趋势敏感性
==========================================================================

M0–M2 的结论靠随机化站住；M3 没有这个靠山。
所以验证台的问题从「我的数算得对不对」变成
「**假设被打破时我会错得多离谱，而且我会不会察觉**」。

### 1. 交错处置：效应处处为正，TWFE 却给出负号
面板：800 个单元 × 7 期
  队列 2: 384 个单元
  队列 4: 384 个单元
  未处置: 32 个单元
真值：整体 ATT = +2.0500
  队列 2 的平均效应 = +3.0000
  队列 4 的平均效应 = +0.6250
  事件研究（相对期数 k -> 真实效应）：
    k= 0: +1.0000
    k= 1: +2.0000
    k= 2: +3.0000
    k= 3: +4.0000
    k= 4: +4.0000
    k= 5: +4.0000

TWFE 隐式权重分解：tau = -0.6710
  处置后单元-期中，权重为负的比例 = 40.0%
  负权重单元承载的真实效应占比 = 73.2%
  权重与真实效应的相关系数 = -0.7853   <- 负相关：权重方向和效应方向相反

按真实效应看平均隐式权重：
      真实效应      单元-期数       平均权重(×1e4)
      0.25        384            5.095
      0.50        384            5.095
      0.75        384            5.095
      1.00        768            8.303
      2.00        384           11.511
      3.00        384           -4.340
      4.00       1152           -4.340

TWFE = -0.6710   CS = +2.1033   真值 = +2.0500
处置前 2 个 lead 的联合 Wald 检验：chi2(2) = 0.220, p=0.8962；未能拒绝 —— 但这**不等于**平行趋势成立（检验对处置后分岔无功效）

### 2. 批量仿真：TWFE vs Callaway-Sant'Anna（200 次）
交错处置：TWFE vs Callaway-Sant'Anna（200 次仿真，真值已知）
  真值均值 = +2.0500
  TWFE: 偏置 -2.7355   RMSE 2.7356   符号翻转率 100.0%
  CS  : 偏置 +0.0016   RMSE 0.0663   符号翻转率 0.0%
  TWFE 隐式权重：处置后负权重占比 40.0%，权重与真实效应相关 -0.785

### 2.5 交互加权聚合，以及**聚合方差**用哪种算法
Sun & Abraham 交互加权（对照组 = not_yet_treated）
  整体 ATT = +2.1033 (SE 0.0574, p=4.506e-173)
  同一组估计、按独立合成的 SE = 0.0340（低估 40.8%）
  事件研究（相对期数 -> ATT，权重 = 该期已处置队列的份额）：
    k= -3: -0.0064 (SE 0.1072) <- 处置前（应接近 0）
    k= -2: -0.0502 (SE 0.1103) <- 处置前（应接近 0）
    k=  0: +0.6569 (SE 0.0607)
    k=  1: +1.2729 (SE 0.0530)
    k=  2: +1.8994 (SE 0.0955)
    k=  3: +2.5934 (SE 0.0686)
    k=  4: +4.0257 (SE 0.1443)
    k=  5: +4.1621 (SE 0.1155)

  与 CS 的关系：在饱和设定下，IW 聚合与 CS 的事件研究**点估计相同**（因为分格估计用的是同一套 2×2）。
    实测：CS 整体 ATT = +2.1033，IW = +2.1033
  真正的差别在**方差的聚合方式**上，下面这组 H0 仿真把它量出来：

H0 下两种聚合方差（200 次仿真，每次 600 单元）
  影响函数合成：越界率 0.0650（α=0.05），覆盖率 0.9350
  独立合成　　：越界率 0.2200，覆盖率 0.7800
  平均 SE：0.0850 vs 0.0477（低估 43.9%）
  处置前（placebo）越界率：0.1250 vs 0.1250（两者通常相同 —— 同一 k 内往往只有一个队列，聚合本身没什么可差的；而这个数**受多重比较影响**，不等于逐系数的 size，要判断整条处置前路径请用 pretrend_test 的联合检验）

  这条差别的意义：整体 ATT 是若干 ATT(g,t) 的加权和，而它们共用对照单元、
  相邻队列还共用基准期 —— 相关性非负。按独立合成算 SE 会低估它，
  于是「名义 5% 的检验」在 H0 下拒绝得远多于 5%。
  **这个 bug 曾经真的在库里**：CS 的整体 SE 就是这么算的，
  而 `_att_influence` 的文档里早就写着「独立合成会严重低估方差」——
  那条教训当时只用在了 lead 的联合检验上，聚合这一步漏掉了。
  现在两条路径都改用影响函数合成，并把「旧算法会是多少」作为诊断一并报出。

### 2.6 Sun-Abraham 回归版：与 IW 版的交叉验证，以及它的边界
  回归版 = 一条回归（队列×相对期数交互项）+ 双向固定效应，
  用**交替投影**吸收固定效应（不构造 N+T 个哑变量，本仓库没有稀疏最小二乘）。
  它与 IW 版走的是完全不同的计算路径，所以两者一致才是强证据：

      相对期数          IW          回归         差     IW SE     回归 SE
        -3     -0.0064     -0.0064   4.4e-14    0.1072    0.1072
        -2     -0.0502     -0.0502   1.9e-14    0.1103    0.1103
         0     +0.6367     +0.6367   3.9e-15    0.0849    0.0849
         1     +1.2758     +1.2758   1.9e-14    0.0520    0.0520
         2     +1.8994     +1.8994   2.9e-14    0.0955    0.0955
         3     +2.5934     +2.5934   4.5e-14    0.0686    0.0686
         4     +4.0257     +4.0257   1.7e-14    0.1443    0.1443
         5     +4.1621     +4.1621   1.2e-14    0.1155    0.1155

  对照组 = never_treated：逐 k 最大点估计差 **4.5e-14**、SE 比值处处 1.000 ——
  「两种算法、同一个估计量」在这里成立。整体 ATT：
    IW +2.099843（SE 0.0658） vs 回归 +2.099843（SE 0.0658），真值 +2.0500
    TWFE 是 -0.6710 —— 效应异质时被负权重拉偏，两个 SA 版本都不偏。

  **但换对照组就不一样了**（这是本轮新测出来的边界，不是实现问题）：
      相对期数          IW          回归         差       贡献队列数
        -3     -0.0064     -0.0064   4.4e-14           1
        -2     -0.0502     -0.0502   1.9e-14           1
         0     +0.6569     +0.6367   2.0e-02           2
         1     +1.2729     +1.2758   2.9e-03           2
         2     +1.8994     +1.8994   2.9e-14           2
         3     +2.5934     +2.5934   4.5e-14           2
         4     +4.0257     +4.0257   1.7e-14           1
         5     +4.1621     +4.1621   1.2e-14           1

  读法：差异只出现在**多个队列共同贡献**的相对期数上，
  单队列贡献的期数（该 k 只有最晚队列还能被观测到）又回到 1e-14。
  原因：IW 的每个 (g,t) 分格用**当时尚未处置**的单元当对照（对照集随 t 变），
  而饱和回归只有一套双向固定效应，已处置队列的变化会进入比较 ——
  这正是 Sun & Abraham 提醒的 forbidden comparison。
  所以：**有未处置组时用回归版（与 IW 等同）**。

  **没有未处置组时怎么办**：原文那一步（用最后一个队列当基准）已经实现。
  Sun & Abraham 自己的 Stata 包把做法写得很具体：用最后处置队列当对照时，
  要**剔除未处置单元**、并且**只保留最后队列被处置之前的期数**。
  实测（600 单元、队列 3/5/7、无未处置组）：
    未处置当基准（默认）：与 IW 的最大逐 k 差 2.393
    最后队列当基准（新）：与 IW 的最大逐 k 差 0.047（改善了约 50 倍）
    整体 ATT：IW +2.0318 vs 新口径 +2.0541（真值 +2.5000）
    代价是**事件窗变短**（最后队列的处置后期数不再可估）——
    这是原文的要求，不是实现妥协；它有测试钉着。


  **第三种估计量：BJS 插补**（只用未处置观测拟合双向固定效应，
  给每条处置观测插补反事实，再对处置观测等权平均）。
    真值 +2.0500 | CS +2.103305（SE 0.0574）| BJS +2.115464（SE 0.0625）
    BJS − CS = +0.012159（2%~4% 量级：**两者不是同一个估计量**）

  为什么差、以及为什么这不是 bug —— 做了一次**诊断**：
  两者只差在**加权方式**（CS/SA 按 (g,t) 分格 2×2 再加权，
  插补对处置观测等权）。判据是：把「效应异质」这个解释排除掉 ——
    同质效应面板 BJS−CS = +0.01365183
    异质效应面板 BJS−CS = +0.01365183
    两者**逐位相同** -> 差异来自加权而不是效应异质（有测试钉着）。

  **第四种估计量：dCDH 换手估计量**（比较「同一批人相邻两期的变化」，
  处置组 = 刚好换手的单元，对照组 = 还没换手的单元）。
    平行趋势成立时：DID+ +0.2135（SE 0.1103）
    逐队列核对（dCDH 的 DID+ vs CS 的同一格 g,t=g，base=g−1，not_yet_treated）：
      g=4: dCDH +0.213508  CS +0.213508  差 +0.0e+00
    聚合后：dCDH +0.2135 vs CS k=0 +0.6569
    **差别不在加权，而在「能用哪些队列」**：dCDH 要多一个前置期做安慰剂，
    所以**最早那个队列被排除**；这份 DGP 的效应逐队列异质，
    少一个队列就会改变加权平均。逐队列上两者是**逐位相同**的。
    安慰剂 DID- = +0.0502（p=0.649）-> 安静
    平行趋势**被违反**时：DID+ +3.4140（真值 +2.0000 —— **同样会偏**）
    安慰剂 DID- = +1.4369（p=7.7e-46）-> **报警**
    这就是这一族估计量真正的价值：点估计并不比别的更抗违反，
    但它**自带一个直接指向违反的安慰剂**（同一批人、同一次比较，
    只把时间往前挪一期）。TWFE 在同样的数据上也会给一个偏的数，
    但不会告诉你假设坏了。
  一句话口径：**有未处置组 -> 默认（1e-14 量级一致）；
  没有 -> base_cohort='last_treated'（差 2.39 -> 0.047）**。

### 2.7 交叉验证抓出来的 bug：处置队列进了自己的对照组
  写回归版时做的逐 k 比对（上面 2.6）暴露了一个**早就存在**的错误：
  `not_yet_treated` 的判据是 `C_i > max(t, g-1)`，而处置队列自己满足
  `g > g-1` —— 于是**处置前的格子里，处置组被算成了自己的对照**。
  后果不是崩溃，而是 placebo 被静默压向 0（对照均值里混进了处置组自身的变化）。

      队列   期    基准      对照(泄漏)      对照(修复)     ATT(泄漏)     ATT(修复)       被压掉
       4   1     3         416          32     -0.0005     -0.0064      92%
       4   2     3         416          32     -0.0039     -0.0502      92%

  处置前 2 个格子：平均 |placebo| 从 0.0022 变成 **0.0283**
  （被压掉 92%）—— 而「处置前系数接近 0」
  看起来**正是我们想看到的结论**，所以这个 bug 一直没被怀疑。
  连带影响：对照掩码重叠让影响函数的两个作用项部分抵消，
  处置前的 SE 被低估一个量级（实测 k=-3 处 0.0082 → 0.1072，13 倍），
  于是「处置前没有异常」这个判断本身也是失真的。

### 2.8 工具变量（2SLS + Anderson-Rubin）：150 次重抽
  横截面上的内生处置：D = pi·Z + u，Y = tau·D + rho·u + e。
  rho=0.8 时 OLS 把混淆记在 D 头上；工具 Z 只通过 D 影响 Y（由构造保证）。

工具变量审计：n=1500，150 次重抽，真实效应 2，内生性 rho=0.8
  pi=0.03  F=   1.19 | OLS 偏差 +0.8040 | 2SLS 中位偏差 +0.5803（占 OLS 的 +72.2%）均值偏差 +0.26 RMSE 4.74 | 覆盖 Wald 0.9733 / AR 0.9933 | AR 中位宽 4.33 无界 0.89 空集 0.00
  pi=0.05  F=   1.77 | OLS 偏差 +0.8036 | 2SLS 中位偏差 +0.4011（占 OLS 的 +49.9%）均值偏差 +0.04 RMSE 6.77 | 覆盖 Wald 0.9667 / AR 0.9867 | AR 中位宽 3.71 无界 0.70 空集 0.00
  pi=0.1   F=   4.55 | OLS 偏差 +0.8021 | 2SLS 中位偏差 -0.0426（占 OLS 的 -5.3%）均值偏差 -0.91 RMSE 4.14 | 覆盖 Wald 0.9667 / AR 0.9733 | AR 中位宽 2.09 无界 0.23 空集 0.00
  pi=0.2   F=  15.75 | OLS 偏差 +0.7962 | 2SLS 中位偏差 -0.0218（占 OLS 的 -2.7%）均值偏差 -0.06 RMSE 0.39 | 覆盖 Wald 0.9800 / AR 0.9600 | AR 中位宽 0.99 无界 0.00 空集 0.00
  pi=0.5   F=  94.59 | OLS 偏差 +0.7566 | 2SLS 中位偏差 -0.0091（占 OLS 的 -1.2%）均值偏差 -0.01 RMSE 0.13 | 覆盖 Wald 0.9933 / AR 0.9600 | AR 中位宽 0.36 无界 0.00 空集 0.00
  正对照（rho=0，没有内生性）：OLS 偏差 +0.0075，2SLS 偏差 -0.0039
  读法（都是量出来的，不是照抄教科书）：
    · OLS 的偏差来自**混淆**，与工具强度无关（每一档都差不多）；
    · 工具越弱，2SLS 的**中位偏差**越朝 OLS 靠，而均值与 RMSE 会爆炸 ——
      「我用了工具变量」救不回点估计，它只是把 OLS 的偏拉回来一部分；
    · **Wald 覆盖率没有崩**（最弱档位仍 ≈0.96）：SE 与尾部一起变大，
      区间宽到盖得住。教科书那句「弱工具让区间过窄」在这个设计下不成立；
    · AR 区间守住了名义覆盖，代价是大部分区间**无界** ——
      正确的话不是「β 在某个窄区间里」，而是「这批数据排除不掉任何 β」。

  单个示例（最弱那一档，看看 AR 区间到底长什么样）：
2SLS：β = +30.1783（稳健 SE 325.8271，Wald 95% 区间 [-608.4310, +668.7876]）
  第一阶段：工具系数 -0.0038（SE 0.0453），稳健 F = 0.01（弱工具，F < 10）
  Anderson-Rubin 区间：**空集** —— 这批数据给不出 95% 可信的 β
  · 第一阶段 F = 0.01 < 10：**弱工具**。此时 2SLS 的**中位偏差朝 OLS 靠**（实测最弱档位到 OLS 偏差的 72%）、均值与 RMSE 会爆炸 —— 而**区间**未必崩：本仓库实测 Wald 覆盖率仍 ≈0.96，崩掉的是 AR 区间的可用性（89% 无界）。两条都要看，见 m3 报告 2.8 节。
  · AR 区间是**空集**：连一个 95% 可信的 β 都给不出来 —— 这本身就是结论，不要退回去看 Wald 区间。

  **这一节的判据被实测改写了一次**（值得单独记）：动手前写的是
  「弱工具会让 Wald 区间覆盖率崩到 0.6 上下」（教科书结论）。
  实测最弱档位（F≈1.2）Wald 覆盖率 **0.9733**，同方差口径与稳健口径
  一模一样 —— 因为 SE 与点估计的尾部**一起**变大，区间宽到盖得住。
  真正崩掉的是三件别的事：中位偏差朝 OLS 靠到 **72.2%**、
  均值偏差与 RMSE 爆炸、AR 区间 **89% 无界**（= 排除不掉任何 β）。
  所以照抄教科书会把报告写错，而量一遍只要 30 秒。

### 3. 平行趋势检验：能发现什么、发现不了什么（300 次/场景）
平行趋势检验的表现（300 次仿真，名义 alpha=0.05）
  size（平行趋势成立时误报）        = 0.0800 [0.0543, 0.1163]
  power（队列专属趋势，处置前可见）  = 1.0000
  power（**处置后才分岔**，处置前不可见）= 0.0800

  有队列专属趋势时 TWFE 偏置 = +0.9336
  处置后才分岔时 TWFE 偏置   = +0.4336
  -> 检验对「处置后才分岔」几乎没有功效：假设已经破了，检验却照常通过。

### 4. 合成控制与安慰剂推断（100 次/场景）
合成控制安慰剂推断（100 次仿真，名义 alpha=0.05）
  无效应时「p < alpha」的比例 = 0.0500 [0.0215, 0.1118]
  有效应时的检出率 = 0.8400
  ATT 偏置 = -0.0231
  处置后/处置前 RMSE 比值分位数 = 1.37 / 2.31 / 2.47

单个示例：
合成控制：ATT = +2.9658
  处置前 RMSE = 0.3316   处置后 RMSE = 3.0159   比值 = 9.094
  有效捐赠单元数 = 5 / 39
空间安慰剂检验（39 个安慰剂）
  真实单元的处置后/处置前 RMSE 比值 = 9.094（排名 1/40）
  安慰剂比值分位数 50%/90%/95% = 1.418 / 1.968 / 2.546
  排名 p 值 = 0.0250   注意：这不是常规 p 值，只是排名的比例，没有渐近分布做后盾

  这一轮补上另外两条检查 —— 三条各自回答**不同**的问题，混着读会读错：
    · 空间安慰剂（上面那条）：别的**单元**会不会也这样 —— 唯一能当检验用的；
    · 时间安慰剂：别的**时段**会不会也这样。它**只看处置前窗口**，
      所以与有没有真实效应**无关**（这一节把「同种子下 H0/H1 逐位相同」
      当成不变量量出来）；它的用途是诊断「这条合成对照在留出的处置前窗口」
      上站不站得住」，不是检验效应；
    · 留一法：结论会不会被某一个捐赠单元撑着。

  SCM 的三条安慰剂检查（各 100 次，alpha=0.05，真实效应 3）
    空间安慰剂：H0 误报率 0.0500，H1 功效 0.8200
    时间安慰剂：不干净的比例 H0 0.1200 / H1 0.1200
      **同种子下 H0/H1 读数逐位相同**的比例 1.0000 —— 它只看处置前窗口，与效应无关
    留一法：H1 下符号稳定 1.0000，中位移动 4.3%
    读法：三条回答三个不同的问题（别的单元 / 别的时段 / 是否靠某一个捐赠单元）；
          只有空间安慰剂能当检验用，另外两条是**诊断**。

  示例上的两条读数：
    时间安慰剂（前 10 期拟合，后 10 期当安慰剂窗口）
      安慰剂窗口的 ATT = +0.0616
      处置前拟合 RMSE = 0.2145 → 标准化后 0.287（≤2 视为干净：True）
      安慰剂窗口内的处置后/处置前 RMSE 比值 = 2.287
    留一法（逐个丢掉 39 个捐赠单元）
      全样本 ATT = +2.9658
      留一 ATT 范围 [+2.8768, +3.0189]，sd 0.0195
      最坏一次移动 3.0%；符号是否稳定：True

### 5. 平行趋势敏感性：翻转点分析（150 次）
平行趋势敏感性（150 次仿真，数据里真的注入了违背）
  实际注入的违背 = +0.6000/期
  翻转点中位数 delta* = +1.3351/期（+0.371 个结果变量 SD/期）
  结论真的扛住的（delta* > 实际违背）比例 = 100.0%
  —— 这才是敏感性分析该回答的问题：**要翻盘得违背到什么程度**

  附：与「处置前趋势」的比值中位数 = 99.87（100% 的样本该比值有限）
  这个比值在处置前趋势平坦时会**爆炸**，本身就不可用 —— 处置前看不到趋势，不代表处置后不会分岔。

单次示例：
平行趋势敏感性（基于 6 个处置后、2 个处置前系数）
  ATT = +2.1033
  **翻转点 delta* = +0.6785/期**（结果变量 SD = 2.4460，即 +0.277 SD/期）
  含义：处置组的反事实轨迹每期比对照组多偏离 delta*，结论就归零。
  与处置前趋势之比 = 36.0 —— **这个数不可用**：处置前趋势近乎平坦，比值被放大；而处置前平坦并不排除处置后分岔。请以 delta* 本身为准。

### 6. 生成图表
  fig16_twfe_negative_weights.png
  fig17_staggered_estimators.png
  fig18_pretrend_blindspot.png
  fig19_scm_placebo.png
  fig20_sensitivity.png

==========================================================================
结论
==========================================================================
[1] 交错处置下 TWFE：偏置 -2.7355，**符号翻转率 100%**
    机制：40% 的处置后单元-期拿到**负权重**，它们承载了 73% 的真实效应，corr(权重, 效应) = -0.785
[2] Callaway-Sant'Anna：偏置 +0.0016，符号翻转率 0%
[3] 平行趋势检验：size 0.0800，对可见违背功效 1.000，对**处置后才分岔**功效 0.080
    -> 假设已经破了，检验却照常通过。这是 M1「校准不等于正确」的姊妹命题：
       **检验通过不等于假设成立**。
[4] 合成控制安慰剂：无效应时假阳性率 0.0500（名义 0.05），有效应时检出率 0.8400
[5] 敏感性分析：实际违背 +0.600/期，翻转点中位数 +1.335/期，结论扛住 100%
[6] 工具变量：OLS 偏差 +0.8040（与工具强度无关），最弱档位（F=1.19）2SLS 中位偏差 +0.5803= OLS 的 +72.2%，RMSE 4.74
    Wald 覆盖 0.9733（**没崩**）而 AR 覆盖 0.9933 —— 代价是 89% 的 AR 区间无界。
    -> 弱工具伤的是**点估计与可用性**，不是覆盖率：
       「我用了工具变量」不是结论，「工具有多强」才是。

逐项检查: {'数据里效应处处为正': True, 'TWFE 出现符号翻转': True, 'TWFE 权重与效应负相关': True, 'CS 基本无偏': True, 'CS 符号翻转率极低': True, '平行趋势检验 size 合理': True, '检验对可见违背有功效': True, '检验对不可见违背无功效': True, '合成控制安慰剂假阳性率接近名义': True, '敏感性分析给出有限翻转点': np.True_, 'SCM 空间安慰剂校准': True, 'SCM 空间安慰剂有功效': True, 'SCM 时间安慰剂与效应无关（不变量）': True, 'SCM 留一法稳健': True, '内生性把 OLS 推偏了': True, '没有内生性时 OLS 无偏（正对照）': True, '弱工具把 2SLS 拉向 OLS': True, 'AR 区间全程守住名义覆盖': True, 'AR 的代价（无界）看得见': True, 'Wald 在这个设计下没有崩（实测记录）': True}
总体判定: PASS
```
