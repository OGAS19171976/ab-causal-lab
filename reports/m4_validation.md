# M4 验证报告

```text
==========================================================================
ab-causal-lab · M4 验证报告
DML · 因果森林 · Uplift 与 Qini
==========================================================================

M4 的验证台比前三个阶段难：DML 的 theta 有真值可比，
但 CATE 没有唯一正确答案 —— 答案取决于 DGP 的函数形式。
所以下面**四种形式各跑一遍**，「没有单一赢家」本身就是结论。

### 1. DML 消掉 naive plug-in 的偏置（每个样本量 15 次仿真）
DML vs naive plug-in（15 次仿真/样本量）
  真 theta = +1.000（constant CATE 下的 ATE）
  n= 1000: naive 偏置 +0.1650 覆盖率 0.60 | DML 偏置 +0.0694 覆盖率 1.00 | 不交叉拟合 +0.0490
  n= 4000: naive 偏置 +0.1862 覆盖率 0.13 | DML 偏置 +0.0142 覆盖率 0.93 | 不交叉拟合 -0.0156
  -> naive 的偏置**不随 n 消失**，覆盖率从 60% 崩到 13%：更多数据只会让有偏的区间更自信地错。
  -> DML 的偏置随 n 收缩（+0.069 -> +0.014），覆盖率守在 93%。
  -> 但注意 n=1000 那一行：nuisance 太弱时 **DML 自己也偏** 它的渐近保证要求 nuisance 收敛快于 n^{-1/4}，小样本 + 弱学习器不满足。

### 2. 因果森林 vs 常数基线（n=3000，四种 CATE 形式）
因果森林 vs 常数基线（n=3000，四种 CATE 形式各一遍）
  注意：这是**留出集**上的 MSE。常数基线 = 对所有人报同一个 ATE。
    constant: MSE 森林   0.6747 vs 常数   0.0000（比值      ∞）  秩相关 +nan  Qini 森林    -0.12 vs 常数   -3.48
      linear: MSE 森林   0.9518 vs 常数   0.8492（比值   1.12）  秩相关 +0.882  Qini 森林   233.96 vs 常数   11.32
   threshold: MSE 森林   0.9068 vs 常数   1.0347（比值   0.88）  秩相关 +0.822  Qini 森林   273.70 vs 常数    2.11
   nonlinear: MSE 森林   1.2669 vs 常数   0.8269（比值   1.53）  秩相关 +0.715  Qini 森林   250.38 vs 常数  -81.74
  -> 弹性模型在至少一种形式下赢过常数基线

### 3. 排序指标与水平指标（n=5000）
排序指标 vs 水平指标（n=5000，nonlinear CATE）
  Qini：样本内 613.66 / 留出 466.37 / 完美 604.83 / 常数 11.83
    样本内高估 +31.6%（把评估样本也拿去拟合，Qini 会虚高）
  MSE：森林 1.0433 vs 常数 0.8948

  排序结论：森林优于常数（Qini 466.4 vs 11.8）
  水平结论：森林劣于常数（MSE 1.043 vs 0.895）
  -> **两个指标给出相反结论**。Qini/AUUC 只衡量排序，完全不衡量水平；
     「我的模型 AUUC 更高」不等于「我的 CATE 估得更准」。

### 3b. CATE 的区间：两条路线的覆盖率（n=1500，5 个场景）
CATE 区间的覆盖率（5 个场景 × n=1500，bootstrap B=25，名义 0.95）
  叶内方差 + 跨树影响函数合成：覆盖率 0.2127，长度 0.7241
  按单元 bootstrap　　：覆盖率 0.3087，长度 1.0364（长度差 30.1%）
  点估计诊断：τ̂ 与真值相关 0.6863，RMSE 0.9894
  真 CATE 的 sd 0.9175 vs τ̂ 的 sd 0.4364；可给区间的单元 77.2%
  → 两条路线的覆盖率都远低于名义值，而**区间长度与真 CATE 的离散度同量级**：所以盖不住的主因是**点估计有偏**。
     （方差本身也曾在跨树上按独立合成而偏小，修法见报告第 8c 节：SE 从真实抽样波动的 0.354 倍抬到 0.702 倍，覆盖率仍上不去 —— 偏差是 SE 的 7.46 倍。）

  验收标准逐条对照（写到报告里，而不是留在脑子里）：
    · 覆盖率 ≈95%（同质 ≥90%）：**不满足** —— 解析 0.2127、bootstrap 0.3087
    · 与 bootstrap 的长度差 ≤30%：**不满足** —— 实测 30.1%
    · 区间长度随 min_leaf 单调收缩：**标准撤回** —— 那是旧口径（只量叶内噪声）的副产品，改成影响函数相加后中位 SE 基本持平；叶子大小换的是偏差不是方差（见 reports/cate_interval_report.md 第 3 节）
    · 明确回答「水平仍不可靠」是否成立：**成立**，且原因已量化
  → 所以第 3 节那句「森林 MSE 反而更差」在加了区间之后依然是结论，
    而现在它多了一个解释：区间盖不住的主因是**点估计有偏**
    （跨树方差本身也曾算错，第 8c 节已修：SE 从真实波动的 0.354 倍到 0.702 倍，
     覆盖率仍上不去 —— 偏差是 SE 的 7.46 倍）。

### 3c. 元学习器对照：S / T / X / R / DR（四种 CATE 形式）
  M4 原先只有 S / T / X 三个元学习器。这一轮补上 **R-learner**（Nie & Wager）
  与 **DR-learner**（Kennedy）：两者都用正交化 + 交叉拟合的 nuisance，
  R-learner 最小化 R-loss（等价于一个加权回归），DR-learner 回归双稳健伪结果。
  线性 τ 时 R-loss 有闭式解；非线性 τ 走「伪结果 + 加权拟合」那条等价路径。

  元学习器对照（n=3000，留出集评估，四种 CATE 形式）
    形式         方法                           MSE       秩相关      Qini
    constant   S-learner                 0.1721       nan    -119.5
    constant   T-learner                 0.4319       nan      34.0
    constant   X-learner                 0.1553       nan      49.5
    constant   R(线性)                     0.0351       nan      72.9
    constant   R(线性, 不交叉拟合)              0.0312       nan      58.7
    constant   R(线性, oracle 倾向)          0.0464       nan      53.1
    constant   R(森林)                     0.2316       nan      46.9
    constant   DR(线性)                    0.0921       nan      -3.9
    linear     S-learner                 0.6671    0.5888     145.6
    linear     T-learner                 0.6753    0.6392     280.7
    linear     X-learner                 0.3185    0.8519     356.8
    linear     R(线性)                     0.0481    0.9734     319.1
    linear     R(线性, 不交叉拟合)              0.0480    0.9736     311.8
    linear     R(线性, oracle 倾向)          0.0744    0.9581     318.7
    linear     R(森林)                     0.3931    0.8200     312.6
    linear     DR(线性)                    0.0966    0.9599     333.5
    threshold  S-learner                 0.5438    0.7687     225.0
    threshold  T-learner                 0.4656    0.7589     271.0
    threshold  X-learner                 0.1923    0.8261     279.0
    threshold  R(线性)                     0.4553    0.7855     259.5
    threshold  R(线性, 不交叉拟合)              0.4618    0.7815     247.2
    threshold  R(线性, oracle 倾向)          0.4790    0.7812     244.6
    threshold  R(森林)                     0.3396    0.8273     260.0
    threshold  DR(线性)                    0.4794    0.7626     235.1
    nonlinear  S-learner                 0.8014    0.6071     198.5
    nonlinear  T-learner                 0.6593    0.6833     279.0
    nonlinear  X-learner                 0.3384    0.8381     333.4
    nonlinear  R(线性)                     0.7417    0.6278     195.1
    nonlinear  R(线性, 不交叉拟合)              0.7248    0.6390     202.9
    nonlinear  R(线性, oracle 倾向)          0.7498    0.6360     198.8
    nonlinear  R(森林)                     0.4068    0.8209     309.6
    nonlinear  DR(线性)                    0.8729    0.4620      95.2
    注：constant 那一档的秩相关是 nan —— 真实 CATE 是常数时「排序」这件事本身不存在，不是实现问题。
    → 每个形式的最小 MSE 方法（**由数据算出来，不写死**）：
      constant   R(线性, 不交叉拟合)（MSE 0.0312）
      linear     R(线性, 不交叉拟合)（MSE 0.0480）
      threshold  X-learner（MSE 0.1923）
      nonlinear  X-learner（MSE 0.3384）
    → **没有单一赢家**：2 个方法各赢至少一种形式（R(线性, 不交叉拟合)、X-learner）。
      这正是本仓库从 M4 起的那条结论 —— 换 DGP 就换赢家，所以「我的模型更好」必须带上「在哪种数据上」。

  三条要一起读的东西：
    · **同基学习器下 R 不差于 T**（四个形式，5% 容差）：True；
    · **没有单一赢家**：每个形式的最小 MSE 方法不同 —— 这是 M4 的老结论；
    · **交叉拟合在 MSE 上不赚**：True（不交叉拟合反而更低）。
      原因是样本内过拟合把 Ỹ 一起缩小、τ̂ 被收缩向 0，相当于正则化；
      交叉拟合买到的是**推断的有效性**（DML 那节量的无偏性与覆盖率），
      不是这个口径上的 MSE。这一条也是**被实测改写过的判据**：
      第一版的对照顺手把不交叉拟合那一支的 ê 换成了常数边际处置率，
      等于一次改了两个变量 —— 对照实验只许改一个。

### 3d. 策略学习：直接优化策略价值（AIPW）
  前面所有 CATE 方法都在回答「谁受益更多」，然后靠排序当策略。
  这一节换一个问题：**把策略价值写成目标函数，直接在策略类上最大化它。**
  口径是相对「谁都不投」的增量 Δ(π) = E[τ(X)π(X)]，用交叉拟合的
  AIPW 伪结果 Γ 估计：Δ̂(π) = mean(π(X)·Γ)，SE 直接来自影响函数。

  策略学习审计（每格 10 次仿真，n=500，候选网格 10）

    一、有信号（真实最优：x₁ > 0 就投）
      oracle 真实价值              +0.5859
      全投的真实价值                +0.1676
      CATE 排序（同份额）真实价值    +0.4113（份额 0.54，达到 oracle 的 70.2%）
      学到的策略（深度1）真实价值    +0.5444（份额 0.54，达到 oracle 的 92.9%）
      学到的策略（深度2）真实价值    +0.5163
      样本内价值（深度1）            +0.6617
      分离样本价值（深度1）          +0.6366
      乐观偏差                      +0.0251

    二、纯噪声（τ ≡ 0：**任何**策略的真实价值都精确等于 0）
      样本内价值·深度1              +0.2499
      样本内价值·深度2              +0.3110
      样本内价值·无限制（Γ>0 就投）  +1.2858
      分离样本价值·深度1            +0.0740（投放份额 0.54）
      预指定策略（x₀>0）的价值       +0.0127   ← 正对照：这就是**估计量本身的偏差**（不是 0，但小一个量级）

    三、偏差曲线（噪声档，只看 x₀ 这一维 —— 它是**下界参照**）：
        预指定阈值 → 跨仿真平均 AIPW 价值
      -1.5  -1.0  -0.5  +0.0  +0.5  +1.0  +1.5
      +0.076  +0.050  +0.010  +0.013  -0.018  -0.021  -0.003
      峰 = +0.076（在阈值 -1.5 处）——
      20 维搜索空间的偏差面只会比这条曲线更高，所以它是下界；
      「选区域」捡到的就是这种系统性偏差（分离样本价值与它同量级），
      而不是「选择噪声」—— 后者在样本分离之后本就应当平均为 0。

    四、同一个估计量，两种用法的覆盖率（τ≡0 时真值 = 0，95% 名义）
      预指定策略（不含选择）        0.9000   ← 正对照
      选中策略·深度1                0.6000
      选中策略·深度2                0.2000
      无限制（Γ>0 就投）            0.0000   ← 崩掉的是「选完再评」这一步

  这一节的四句话：
    · **学到的策略是真的**：真实价值达到 oracle 的 92.9%，是全投基线的 3.2 倍；
    · **样本内价值不是价值**：τ≡0 时它仍然给出 +0.250（无限制那一支 +1.286，真值精确为 0）；
    · **分离样本只治一半**：它把 +0.250 压到 +0.074，
      剩下的与预指定偏差曲线的峰（+0.076）同量级 ——
      **分离治的是选择偏差，不治 nuisance 偏差**；
    · **覆盖率那两栏要一起读**：预指定策略 0.90，深度1 0.60，深度2 0.20，无限制 0.00 ——
      同一个估计量、同一批数据，差别只在「有没有选过」。
  还有一条要写进报告而不是留在读者以为里：正对照（**预指定**策略）
  的价值是 +0.0127 而不是 0 ——
  这就是这套 nuisance 在这个 n 下的偏差本身；它的区间仍然覆盖 0，
  但那是因为 SE 更大 —— **偏差小不等于偏差不存在**。

### 4. 生成图表
  fig21_dml_bias.png
  fig22_cate_forms.png
  fig23_ranking_vs_level.png
  fig24_uplift_curve.png

==========================================================================
结论
==========================================================================
[1] DML 消偏置（n=4000）：naive 偏置 +0.1862，95% CI 覆盖率只有 13%；DML 偏置 +0.0142，覆盖率 93%
    naive 的偏置**不随 n 消失**：覆盖率从 n=1000 的 60% 崩到 n=4000 的 13%
    -> 更多数据只会让有偏的区间更自信地错
    诚实的边界：n=1000 时 DML 自己也偏（+0.069）—— nuisance 太弱时不满足 n^{-1/4} 收敛条件
[2] 四种 CATE 形式下，弹性模型的 MSE 相对常数基线分别是 ∞ / 1.12 / 0.88 / 1.53
    弹性模型只在 threshold 下赢过常数基线；在 constant, linear, nonlinear 下都输
    -> 它只在自身归纳偏置恰好匹配 DGP 时才赢 —— 而你在真实数据里并不知道 DGP 是哪一种
[3] 秩相关 +0.715 说明森林**确实抓到了排序**，只是水平的方差太大
[4] Qini：留出集森林 466.4 vs 常数 11.8（完美 604.8）；样本内虚高 +32%
    -> **排序指标与水平指标给出相反结论**。
       「我的模型 AUUC 更高」不等于「我的 CATE 估得更准」。
[5] 元学习器：同基学习器下 R 不差于 T = True；每个形式的最小 MSE 方法：constant→R(线性, 不交叉拟合)(0.031)、linear→R(线性, 不交叉拟合)(0.048)、threshold→X-learner(0.192)、nonlinear→X-learner(0.338)
    -> **没有单一赢家**（M4 的老结论），而**交叉拟合在 MSE 上不赚**：
       它买到的是推断的有效性，不是这个口径上的点估计。
[6] 策略学习（3d）：学到的策略真实价值是 oracle 的 92.9%；而 τ≡0 时样本内价值仍有 +0.250（真值 0），分离样本后 +0.074，与偏差面峰同量级
    -> **策略学习的头条数字必须是分离样本价值**；「样本内提升了 X」只说明搜索空间有多大。
       覆盖率的对照更直白：预指定策略 0.90 vs 无限制类 0.00。

逐项检查: {'naive plug-in 有可见偏置': True, 'DML 偏置远小于 naive': True, 'DML 覆盖率守住在名义附近': True, 'naive 覆盖率随 n 崩塌': True, '四种 CATE 形式都跑过': True, '存在排序能力（秩相关为正）': True, '排序与水平结论冲突': True, '样本内 Qini 虚高': True, 'R(森林) 不差于 T-learner（同基学习器，四个形式）': True, '元学习器没有单一赢家': True, '交叉拟合在 MSE 口径上不赚（实测记录）': True, '策略学习：噪声档：样本内价值为正（真值恒为 0）': True, '策略学习：噪声档：容量越大样本内价值越高': True, '策略学习：噪声档：不限制策略类最乐观': True, '策略学习：噪声档：分离样本价值显著变小': True, '策略学习：正对照：预指定策略的偏差远小于样本内价值': True, '策略学习：正对照：预指定策略的区间守住覆盖': True, '策略学习：机制：偏差曲线不是平的（选区域能捡到）': True, '策略学习：机制：分离样本价值与偏差面同量级': True, '策略学习：大容量类的区间排除真值（覆盖率崩）': True, '策略学习：有信号：真实价值明显高于全投': True, '策略学习：有信号：达到 oracle 的 80%': True, '策略学习：有信号：乐观偏差很小': True}
总体判定: PASS
```
