# 差异审计：合成数据 vs 真实外部数据（MovieLens）

同一组公式、同一条链路，两个仓库各算一遍。
读法：**该一致的是方法学性质**（SRM 校准、CUPED 不放大方差、A/A 不显著），
**该不一致的是分布性质**（零膨胀、峰度、极值集中度）。

| 读数 | 合成（warehouse.duckdb） | 真实（warehouse_real.duckdb） |
|---|---|---|
| 用户数 | 47946 | 610 |
| 结果指标：均值 | 576.5428 | 0.0000 |
| 结果指标：SD | 189.9614 | 0.0000 |
| 结果指标：偏度 | +0.229 | +nan |
| 结果指标：峰度 | +0.008 | +0.000 |
| 零值占比 | 0.0000 | 1.0000 |
| 最大值 / 均值 | 2.4 | nan |
| CUPED 方差缩减（加权） | +0.6295 | +nan |
| θ（合并） | +0.8022 | +nan |
| 主效应 τ̂（post-only） | -8.3033 | +nan |
|   z（post-only） | -2.381 | +nan |
|   p（post-only） | 0.0173 | nan |
| 主效应 τ̂（CUPED） | -3.1470 | +nan |
|   z（CUPED） | -1.482 | +nan |
|   p（CUPED） | 0.1382 | nan |
| SRM χ²（最大） | 77.6258 | 0.6557 |
| 按日累计 z：天数 | 21 | 0 |
| 按日累计 z：max|z| | 13.113 | nan |
| 按日累计 z：最后一天 | +13.113 | +nan |
| 簇级日表行数 | 6278 | 594 |
| 不同簇数 | 60 | 1 |
| 有 true_lift 的行数 | 6 | 0 |

> **注意：有一侧的指标是退化的**（方差为 0 / 全 0）—— 见下面第 0 条。

## 结论（哪些变了、哪些没变）

0. **第一次跑就抓到一个真问题**：真实侧的 pre/post 指标全是 0。
   根因不在数据：数仓 SQL `sql/01_dwd_experiment_user.sql` 第 59 行**写死**了
   `ev.event_name = 'interaction'`（合成器的事件名），而真实数据的事件叫
   `rating` —— 于是 exposure 计数正常、SRM 正常、**指标却是空的**。
   这正是「接入外部数据」该买到的东西：契约与反冒充都过了，
   但**链路里的假设**（事件名）只有换成真数据才会暴露。
   下一轮做：把事件名从声明传到 SQL（参数化），并让门在「指标全 0」时直接报红。

1b. **该一致的一致（本次只做到一半）**：分流的 SRM 两侧都正常（真实 χ² = 0.6557），但真实侧的 CUPED 与主效应**算不出来** ——
   因为指标是空的（见第 0 条）。所以「合成 vs 真实」这一步现在只完成了
   分流那一半，方差与口径那一半要等事件名接上之后重跑。

1. ~~该一致的一致~~（见 1b：本次只有 SRM 那一半）

   χ² = 0.6557，不显著 ⇒ 分流没坏）；CUPED 在两个仓库上都
   CUPED 方差缩减：合成 +0.6295；真实侧本次**算不出**（指标为空，见第 0 条）。

2. **该不一致的不一致**（这一份报告真正买到的东西）：
   分布形状差得很远 —— 零值占比 0.0000 → 1.0000，峰度 +0.008 → +0.000，最大值/均值 2.4 → nan。
   合成器的分布是我们写进去的，所以它的方差性质偏**乐观**；
   真实数据这一列才是「方差估计会不会太窄」的参照。

3. **真实数据带出来的两个结构性限制**（不是 bug，是边界）：
   真实侧的簇级日表只有 **1 个簇**（合成侧 60 个）——
   因为 MovieLens 没有 `city`，簇级聚合退化成一个占位簇
   （列在契约里是可选，但「可选」不等于「能算簇级」）；
   真实侧的 `true_lift` 行数 = **0**（合成侧 6）——
   外部数据没有演示真值，所以它只能做校准，不能做「效应估得准不准」的验收。

4. **序贯视角只能看量级**：按日累计 z 的 max|z| 真实 nan / 合成 13.113（天数 0 / 21）——
   这是**一次实现**，不是 FWER；真实的 FWER 要用重随机化（本仓库在合成数据上做过，
   见 m2 报告），外部数据上做不了，因为分流是我们自己做的 A/A。

